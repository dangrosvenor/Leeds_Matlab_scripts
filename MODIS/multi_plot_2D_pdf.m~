lat_range=-80:-70;

for ilat_range=1:length(lat_range)
    LAT_val = [lat_range(:-76]; LON_val = [-120:-90]; %Antarctica, random region
    plotTimeHeightVap3
    saveas_ps_fig_emf(gcf,[savename]);
    close(gcf);
end

