function [out]=UM_calc_lat_mean(lat2d,lon2d,dat,lat_range,nlat)

dlat = (lat_range(2)-lat_range(1) ) / nlat;

lats=[lat_range(1):dlat:lat_range(2)];

for ilat=1:length(lats)
    ii = find(lat2d>lats(ilat) & lat2d<=lats(ilat)



